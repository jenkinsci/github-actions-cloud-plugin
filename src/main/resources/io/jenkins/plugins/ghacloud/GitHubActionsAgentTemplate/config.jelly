<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">

    <f:entry title="${%Labels}" field="labelString">
        <f:textbox />
    </f:entry>

    <f:entry title="${%Remote FS Root}" field="remoteFs">
        <f:textbox default="/home/runner/agent" />
    </f:entry>

    <f:entry title="${%Number of Executors}" field="numExecutors">
        <f:textbox default="1" />
    </f:entry>

    <f:entry title="${%Git Ref}" field="gitRef">
        <f:textbox default="main" />
    </f:entry>

    <f:entry title="${%Idle Termination Minutes}" field="idleMinutes">
        <f:textbox default="5" />
    </f:entry>

    <f:entry title="${%Workflow File Name (optional, overrides cloud default)}" field="workflowFileName">
        <f:textbox placeholder="jenkins-agent-windows.yml" />
    </f:entry>

    <f:entry>
        <div align="right">
            <f:repeatableDeleteButton />
        </div>
    </f:entry>

</j:jelly>
